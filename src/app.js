import React from 'react';
import './app.sass';

const App = () => {
  const theme = 'black'

  return (
    <div id='app' className={theme}>
      <h1>React Template</h1>
    </div>
  );
};


export default App
